package chuDeJSON;

public class nhap {
	/*
	 * JSONParser jsp = new JSONParser(); JSONObject jso = (JSONObject)
	 * jsp.parse(new FileReader("danhsachbook.json")); JSONArray jsa = (JSONArray)
	 * jso.get("book"); for (Object jsi: jsa) { JSONObject jso2 = (JSONObject) jsi;
	 * String language = jso2.get("language").toString(); String edition =
	 * jso2.get("edition").toString(); System.out.println(String.format("%s: %s",
	 * language, edition)); };
	 */
}
